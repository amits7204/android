<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:theme="@style/SwitchTheme"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/normal_life"
            android:textColor="@color/colorPrimary"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="20dp"/>

        <Switch
            android:id="@+id/switch_work"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/job"
            android:textStyle="bold"
            android:theme="@style/SwitchTheme"
            android:switchPadding="18dp"
            android:textOff="@string/off"
            android:textOn="@string/on"
            android:layout_marginBottom="10dp"/>

        <Switch
            android:id="@+id/switch_social"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/social_life"
            android:textStyle="bold"
            android:theme="@style/SwitchTheme"
            android:switchPadding="18dp"
            android:textOff="@string/off"
            android:textOn="@string/on"
            android:layout_marginBottom="10dp"/>

        <Switch
            android:id="@+id/switch_sleep"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/sleep"
            android:textStyle="bold"
            android:theme="@style/SwitchTheme"
            android:textOff="@string/off"
            android:textOn="@string/on"
            android:switchPadding="50dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/made_by_amit_singh"
            android:gravity="center"
            android:textStyle="bold"
            android:textColor="@color/grayfifty"
            android:layout_marginTop="20dp"/>
    </LinearLayout>

</android.support.constraint.ConstraintLayout>